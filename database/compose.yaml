# Use postgres/example user/password credentials
version: '3.1'

services:
  db:
    build: .
    restart: always
    environment:
      POSTGRES_USER: pg
      POSTGRES_PASSWORD: azerty
    ports:
      - "5433:5432"